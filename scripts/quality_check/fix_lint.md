The lint check (`cargo clippy -- --deny warnings`) failed.

Read the clippy output below to understand the warnings, then fix them in the source code.
Do not suppress warnings with `#[allow(...)]` unless there is no reasonable fix.
After fixing, verify with `cargo clippy -- --deny warnings`.
