The test suite (`cargo test`) failed.

Read the test output below to understand which tests failed and why, then fix the code so all tests pass.
Do not delete or skip failing tests unless the test itself is incorrect.
After fixing, verify with `cargo test`.
